<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= pageTitle %> - Medication Tracker
    </title>
    <link rel="icon" href="/Star_of_life2.svg.png" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
    <%- include('partials/nav') %>

        <main class="max-w-6xl mx-auto mt-10 px-4">
            <h1 class="text-4xl font-bold text-center mb-12">Our Features</h1>

            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% features.forEach(feature=> { %>
                    <div class="bg-white rounded-xl shadow-md p-6 hover:shadow-lg transition-shadow">
                        <div class="text-center">
                            <i class="fas <%= feature.icon %> text-4xl text-blue-600 mb-4"></i>
                            <h3 class="text-xl font-semibold mb-2">
                                <%= feature.title %>
                            </h3>
                            <p class="text-gray-600">
                                <%= feature.description %>
                            </p>
                        </div>
                    </div>
                    <% }) %>
            </div>

            <div class="mt-16 bg-blue-600 text-white rounded-xl p-12 text-center">
                <h2 class="text-3xl font-bold mb-4">Ready to Experience These Features?</h2>
                <p class="mb-8 text-xl">Join thousands of users who are already benefiting from our platform</p>
                <a href="/register"
                    class="bg-white text-blue-600 px-8 py-3 rounded-lg hover:bg-gray-100 transition-colors inline-block">
                    Get Started Free
                </a>
            </div>
        </main>
</body>

</html>